---
export interface Props {
  skill: string;
}

const { skill } = Astro.props;
---
<span class="mr-1.5 mt-2 inline-flex items-center rounded-md bg-gray-400/10 px-2 py-1 text-xs font-medium text-gray-400 ring-1 ring-inset ring-gray-400/20">{skill}</span>